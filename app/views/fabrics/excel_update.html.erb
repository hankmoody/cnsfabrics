<div class="row">
  <div class="col-md-6">
    <h2>Upload Results</h2>
    <table class="table table-striped">
      <% @status.each do |s| %>
        <tr class="<%= s.keys.first.to_s %>">
          <td><%= s.values.first %></td>
        </tr>
      <% end %>
    </table>
  </div>
</div>
